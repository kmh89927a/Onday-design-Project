---
name: Feature Task
title: "[Feature] API-005: SavedSearch 도메인 Request/Response DTO — saveSearch(), getSavedSearch() + SavedSearchErrorCode (5종) + SAVED_SEARCH_ERROR_MAP + createSavedSearchError helper. ★ Wave 2 트랙 D 마지막 마일스톤 (API-001→002→003→005 완성 — 본 ISSUE 머지 시 Critical Path Wave 3 진입). ★ Q3 (가) 2 파일 분리 strategy 첫 코드 입증 (DB-006 §7 직접 충족 + DB-003 Q6 패턴 답습). ★ 3종 가이드 § 완성 ((P1) 미묘 구분 + 분리/통합 strategy + mapper 패턴 차이 — Wave 3+ 새 도메인 ISSUE 결정 매트릭스)"
labels: ['feature', 'priority:L', 'epic:API Contract', 'wave:2']
assignees: []
---

## 1. 🎯 Summary

- **기능명:** [API-005] SavedSearch 도메인 Request/Response DTO 정의 — saveSearch() / getSavedSearch() 통신 계약 + SavedSearchErrorCode (5 종) + SAVED_SEARCH_ERROR_MAP + createSavedSearchError helper. ★ **API Contract 트랙 네 번째 진입 — Wave 2 트랙 D 마지막 마일스톤** (Wave 2 = API-001 PR #81 + API-002 PR #82 + API-003 PR #83 직후, 본 ISSUE 머지 시 Critical Path Wave 3 진입 — MOCK-001~005 / TEST-001~008 / CMD-DIAG·AUTH·SHARE·SAVE / QRY-DIAG·SHARE·SAVE). 본 ISSUE 산출물 = **신규 4 코드 + 명세 1 파일 갱신** + **커밋 2개 (feat + docs 분리, API-001/002/003 패턴 정확 답습)**. Zod (§3.3) + mapper (§3.5) + spec (§3.6/3.7/3.8) + DB-006 §9.1 strict/passthrough 결정 + userId 서버 사이드 추출 (REQ-NF-021) 은 호출처 인라인 동작 중 / 영역 분리 원칙 (DB-006 Q3 / API-001 Q6 / API-002 Q8·Q9 / API-003 Q7·Q8 일관) 으로 위임. ★ **saved-search.ts (DB-006 영역 base) 신규 첫 owner** — DB-006 §7 위임 트리거 직접 수신 (DB-004→API-003 패턴 두 번째 답습). ★ **Q3 (가) 2 파일 분리 strategy — saved-search.ts (DB-006 영역 base) + saved-search-api.ts (API-005 영역) — DB-003 Q6 패턴 답습** (diagnosis.ts base + diagnosis-errors.ts API 분리 평행) + 명세 §3.1 분리 의도 정합 + ★ **분리/통합 strategy § 신규 owner** (3종 가이드 § 중 2번). ★ **Q2 가드 3회째 사수** — `types.ts:1-3` 무수정 + DiagnosisMode 11 + DiagnosisStatus 2 호출처 본 ISSUE 미치환 + **DiagnosisMode/ServiceModeType forward 0건** (★ API-003 F1+F2 2건 보다 더 약함 — 가장 약한 의존성 입증). ★ **ServiceModeType 재사용 4회째** (user.ts 정의 + diagnosis.ts API-002 + share-link.ts API-003 + ★ saved-search.ts API-005 = 4회 일관 import 패턴). ★ **도메인별 mapper 패턴 차이 § 신규 owner** (3종 가이드 § 중 3번) — API-002 N3 single (Route only) / API-003 S3+S4 dual (Route+SSR 단일 owner) / ★ **API-005 SS1 L55-61 single (Route only) = QRY-SAVE-001 single owner** 3 패턴 비교 (미래 새 도메인 mapper 결정 가이드). ★ **DB-006 §7 + §9.1 동시 트리거 수신** — §7 (saved-search.ts 신규 owner + SearchParams + SavedSearchDTO + ServiceModeType import, DB-003 Q6 답습) 직접 충족 + §9.1 (strict/passthrough 결정 = CMD-SAVE-001 영역) 위임으로 받음 = DB-006 단일 ISSUE 가 두 트리거 동시 발신 → 본 ISSUE 동시 수신. ★ **userId 클라이언트 전달 금지 (REQ-NF-021)** — SaveSearchRequest 에 userId 필드 절대 금지, Supabase Auth 세션 서버 사이드 추출 (CMD-SAVE-001 영역 추출 패턴, 본 ISSUE = DTO contract only 명시).
- **목적 (Why):**
  - **비즈니스:** 반복 이사자 (C-04) 의 입력값 자동 저장·복원 기능 (REQ-FUNC-025 best effort) 의 API 통신 계약 (Request/Response DTO + 에러 매핑 + helper + base type) 을 단일 진실 공급원 (SSoT) 으로 확립하여, 후속 CMD-SAVE-001 + QRY-SAVE-001 + TEST-005 (저장 GWT) + TEST-007 (간이 시나리오) + UI-014 가 일관된 타입으로 import 한다. ★ API Contract 트랙 = **DTO 정의 자체가 본 ISSUE 의 owner** → docs only 시 빈 contract 로 Wave 2 진입 의미 상실. ★ 단 Zod (`saveSearchRequestSchema`) + mapper (`mapSavedSearchToDTO` + `mapToGetResponse`) 는 현재 인라인 동작 중 (`api/save/route.ts:5-7` `z.record(z.string(), z.unknown())` passthrough-like + L13 safeParse 호출처 활성 / L55-61 인라인 flat mapper) → 본 ISSUE 가 추출 시 동작 중 인라인 가드 위반 + SS1 우회 (DB-006 Q3 / API-002 §3.5 / API-003 §3.5 인라인 보존 패턴 답습). spec 도 vitest config 부재 (DB-001~007 + API-001/002/003 일관) → TEST-005/007 위임. ★ **Q3 (가) 2 파일 분리 strategy 첫 코드 입증** — 명세 §3.1 자체가 saved-search.ts (base) + saved-search-api.ts (API) 2 파일 분리 의도 명시 + DB-006 §7 직접 명시 ("API-005 = `src/lib/types/saved-search.ts` 신규 + SearchParams + SavedSearchDTO + ServiceModeType import + DB-003 Q6 답습") + DB-003 Q6 패턴 (diagnosis.ts base + diagnosis-errors.ts API 분리) 평행 → 본 ISSUE 가 base/API 분리 직접 충족 + 향후 새 도메인 가이드 §. ★ **3종 가이드 § 완성** — (P1) 패턴 미묘 구분 (API-003 §) + 분리/통합 strategy (★ API-005 § 신규) + mapper 패턴 차이 (★ API-005 § 신규) = Wave 3+ 새 도메인 ISSUE 결정 매트릭스. ★ Q2 (A) — SearchParams.mode = ServiceModeType (`from './user'`) 사용 (DiagnosisMode 미사용, 9칸 가드 ★ 11번째 사수). DiagnosisMode/ServiceModeType forward 0건 grep 검증 (★ API-003 F1+F2 2건 보다 더 약함 — 가장 약한 의존성 확정).
  - **사용자 가치:** 세션 종료 시 입력 조건 자동 저장 → 다음 방문 시 1초 이내 복원 (REQ-FUNC-025) 의 인프라가 API 통신 계약 레벨에서 확보됨. best effort 저장 실패 시 UI 가 에러를 노출하지 않는 패턴 (REQ-NF-016) 이 타입 레벨에서 지원. Geocoding 재검증 결과 (`GeocodingValidationStatus`) 가 옵셔널 필드로 정의되어 "주소를 다시 입력해주세요" 안내 시점이 명확.
- **범위 (What):**
  - ✅ 만드는 것 (신규 4 코드 파일 + 명세 1 파일):
    - **`onday-app/src/lib/types/saved-search.ts`** (신규, **DB-006 영역 base 첫 owner**) — SearchParams (mode: ServiceModeType + leisure 4 필드 + filters DiagnosisFilters 평행) + SavedSearchDTO (userId/searchParams/savedAt). **import 부 정확 1 (정직 기록): `import type { ServiceModeType } from './user'` — Prisma 0, types.ts DiagnosisMode/DiagnosisStatus 0**.
    - **`onday-app/src/lib/types/saved-search-api.ts`** (신규, **API-005 영역**) — SaveSearchRequest (★ userId 필드 절대 금지, REQ-NF-021) + SaveSearchResponse + GetSavedSearchResponse + GeocodingValidationStatus + SavedSearchErrorCode 5종 + SavedSearchErrorDTO. **import 부 정확 1: `import type { SearchParams, SavedSearchDTO } from './saved-search'` (base 에서 import, 재정의 0) — Prisma 0**.
    - **`onday-app/src/lib/constants/saved-search-errors.ts`** (신규) — `SAVED_SEARCH_ERROR_MAP` (5 키 × {message, httpStatus}).
    - **`onday-app/src/lib/helpers/saved-search-error.ts`** (신규) — `createSavedSearchError(code, originalError?)` helper (API-001/002/003 §3.6 createXError 패턴 일관 4 회째).
    - **`tasks/API-005.md`** — 본 명세 현실 동기화 (Q1~Q10 + Q3 (가) 2 파일 분리 strategy § + 3종 가이드 § 완성 + Q2 가드 3회째 사수 표 17행 + SS1~SS3 + ★ 도메인별 mapper 패턴 차이 § + ★ Wave 2 트랙 D 완성 마일스톤 § + ★ DB-006 §7+§9.1 동시 트리거 수신 § + ★ Wave 2 체인 작동 4회째 § + §9 follow-up 7+).
  - ❌ 만들지 않는 것 (인라인 동작 중 dead 회피 + Q2 가드 + 영역 분리 + DB-006 §9.1 = CMD-SAVE-001):
    - `lib/validators/saved-search.ts` 신규 (§3.3 `saveSearchRequestSchema`) — **CMD-SAVE-001 위임** (SS1 `api/save/route.ts:5-7` `z.record(z.string(), z.unknown())` passthrough-like 인라인 동작 중 + L13 safeParse 호출처 활성 + ★ **DB-006 §9.1 strict/passthrough 결정 트리거 본 ISSUE 수신 → CMD-SAVE-001 위임으로 발신**, DB-006 Q3 / API-002 Q8 / API-003 Q7 인라인 보존 패턴 답습)
    - `lib/mappers/saved-search-mapper.ts` (§3.5 `mapSavedSearchToDTO` + `mapToGetResponse`) — **QRY-SAVE-001 single owner 위임** (★ SS1 `api/save/route.ts:49 findUnique + L55-61 인라인 mapper`, Route only single 패턴 — **API-003 dual 과 다름, API-002 single 과 동일**)
    - `__tests__/types/saved-search-api.spec.ts` + `__tests__/validators/saved-search-api.spec.ts` + `__tests__/mappers/saved-search-mapper.spec.ts` — **TEST-005 (저장 GWT) + TEST-007 (간이 시나리오) 분배 위임** (vitest config 부재)
    - **DB-006 §9.1 strict/passthrough 결정** — **CMD-SAVE-001 영역** (DB-006 §9.1 자체가 CMD-SAVE-001 위임 명시 + SS1 `z.record(z.string(), z.unknown())` passthrough 변형 보존 + 본 ISSUE = DTO contract only).
    - **userId 클라이언트 전달** (SaveSearchRequest 에 userId 필드 절대 금지, REQ-NF-021) — Supabase Auth 세션 서버 사이드 추출. **CMD-SAVE-001 영역** (본 ISSUE = DTO contract only, 주석으로만 명시).
    - `types.ts:1-3` `AuthProvider`/`DiagnosisMode`/`DiagnosisStatus` 통합 정리 — **Q2 가드 3회째 사수 (API-002 첫 성공 / API-003 2회째 / 본 ISSUE 3회째 일관)**. owner = 별도 cleanup ISSUE (REFACTOR-L6 또는 MOCK-005 연계, API-002 §9.4 트리거)
    - `DiagnosisMode` 11 호출처 + `DiagnosisStatus` 2 호출처 일괄 치환 — Q2 cleanup ISSUE 위임 (API-002 §9.4 일관)
    - **★ forward 0건 확정** — SavedSearch 영역에 DiagnosisMode/ServiceModeType direct import 0건 + forward 0건 (★ API-003 F1+F2 2건 보다 더 약함, 가장 약한 의존성)
    - SS1~SS3 SavedSearch 현실 코드 (`api/save/route.ts` POST+GET / `lib/db.ts` 7 호출처 mock storage / `validators/saved-search.ts` 부재) — 본 ISSUE 미수정 (후행 CMD-SAVE / QRY-SAVE / INFRA-002 통합 대상)
    - S1~S6 ShareLink 현실 코드 (API-003 가드 누적) + N1~N6 Diagnosis 현실 코드 (API-002 가드 누적) + mock-auth M1~M4 (API-001 가드 누적) — 본 ISSUE 무관여
    - API-001 산출물 (`auth.ts` + `auth-errors.ts` + `auth-error.ts`) + API-002 산출물 (`diagnosis.ts` + `diagnosis-errors.ts` + `diagnosis-error.ts`) + API-003 산출물 (`share-link.ts` + `share-link-errors.ts` + `share-link-error.ts`) — 본 ISSUE 미수정
    - `SavedSearchDTO = PrismaSavedSearch` 등 **Prisma model re-export** — **(P1) 패턴 오해 금지**. (P1) 정신 = 기존 도메인 entity 재사용 (`OAuthProvider=AuthProviderType` / `CandidateAreaDTO=CandidateArea`) 이지 Prisma 직접 노출 아님. ★ DB-006 base type 0 (Q7/Q8 DTO=API-005 위임) → API-003 share-link.ts 패턴 평행 = 자기 도메인 신규 정의 정상 ((P1) 적용 불가).
    - `DiagnosisMode` / `ServiceModeType` / `SearchParams` / `SavedSearchDTO` 재정의·중복 정의 — **절대 금지** (3+중 중복 원인 차단, 본 ISSUE 의 saved-search.ts ↔ saved-search-api.ts 분리 가드 핵심 — saved-search-api.ts 에서 SearchParams/SavedSearchDTO 재정의 시 책임 혼재 + 명세 §3.1 분리 의도 위반)
- **복잡도:** L (명세 v1.0 동일)
- **Wave:** 2 (API Contract 트랙)

---

## 2. 🔗 References (Spec & Context)

### SRS 인용 (REQ ID + 본문 발췌)

- **REQ-FUNC-025** (§4.1.5): "시스템은 사용자가 진단을 완료하고 세션 종료 또는 앱 종료 시 입력 조건(주소·필터·시간대)을 자동 저장해야 한다. 저장은 best effort로 처리하며 실패 시 사용자에게 알리지 않는다. 다음 방문 시 복원 시간은 1초 이내여야 하며, 불러온 주소가 geocoding 실패 시 \"주소를 다시 입력해주세요\" 안내를 표시한다."
- **REQ-NF-016** (§4.2.2): "입력값 자동 저장 성공률 — Best effort (저장 실패 시 무시, 사용자 미통지)"
- **REQ-NF-021** (§4.2.3): "비인가 제3자 접근 시 개인정보 노출 0건" — ★ 본 ISSUE = SaveSearchRequest 에 userId 클라이언트 전달 절대 금지 (서버 사이드 Supabase Auth 세션 추출).
- **REQ-NF-035** (§4.2.6): "에러 로그 알림 — Sentry 기본 알림 설정 사용 (커스텀 슬랙 임계치 제거)"

### `prototypes/design/CLAUDE.md` §3 (1인 MVP 제약) 인용

- "Vercel Serverless Timeout = 10초" → 본 ISSUE 비대상 (API Contract 트랙, DTO only).
- "Out of Scope ... 자동 코드 생성 금지" → §3.3 Zod 인라인 동작 중 + §3.5 mapper 인라인 동작 중 + DB-006 §9.1 결정 본 ISSUE 미수정 = 미리 추출 시 dead 또는 인라인 우회 (DB-006 Q3 / API-001 Q6 / API-002 Q8·Q9 / API-003 Q7·Q8 일관)

### API 엔드포인트 (§6.1)

| # | Method | Endpoint / Action | 요청 Body | 응답 Body | 응답 시간 |
|---|---|---|---|---|---|
| API-05 | POST | `saveSearch()` (현실 `POST /api/save`) | `searchParams` (★ userId 서버 사이드 추출, 클라이언트 전달 금지 REQ-NF-021) | `savedSearchId`, `savedAt` | ≤ 500ms |
| API-05 | GET | `getSavedSearch()` (현실 `GET /api/save`) | (세션 userId) | `saved` (savedSearch \| null) + geocodingStatus? | ≤ 1초 (REQ-FUNC-025 복원 시간) |

### ERD 컬럼 vs Prisma model SavedSearch (§6.2.5 SAVED_SEARCH — 현실 추인)

| 필드 | SRS | Prisma model SavedSearch (DB-006 PR #79 머지) |
|---|---|---|
| `id` | (명세 미표기, 현실 추인) | `String @id @default(uuid())` |
| `user_id` | UUID FK → USER.id, PK (UNIQUE) | `String @unique @map("user_id")` (1:0..1) |
| `search_params` | JSONB NOT NULL | `String @map("search_params") // JSON string` (SQLite 미지원 — DB-006 §9.1 Postgres 전환 시 `Json @db.JsonB`) |
| `saved_at` | TIMESTAMP NOT NULL DEFAULT NOW() | `DateTime @default(now()) @map("saved_at")` |
| FK CASCADE | (관계) | `user User @relation(...) onDelete: Cascade` |

### ★ Phase B 작성 산출물 표 (신규 4 파일 — API-003 +1 패턴)

| # | 파일 | 역할 | 명세 §3 매핑 |
|---|---|---|---|
| **B.1** | `onday-app/src/lib/types/saved-search.ts` (★ DB-006 영역 base, 신규 첫 owner) | SearchParams (mode: ServiceModeType + leisure 4 필드 + filters DiagnosisFilters 평행) + SavedSearchDTO (userId/searchParams/savedAt). **import 부 정확 1 (정직 기록): `import type { ServiceModeType } from './user'` — Prisma 0, types.ts Mode/Status 0**. ★ Request/Response/Error 정의 절대 금지 (saved-search-api.ts 영역). | §3.1 base (DB-006 §7 직접 충족) |
| **B.2** | `onday-app/src/lib/types/saved-search-api.ts` (★ API-005 영역, 신규) | Request 1 (★ userId 필드 금지 REQ-NF-021) + Response 2 + GeocodingValidationStatus + SavedSearchErrorCode 5종 + SavedSearchErrorDTO. **import 부 정확 1: `import type { SearchParams, SavedSearchDTO } from './saved-search'` (base 에서 import, 재정의 0)**. ★ SearchParams/SavedSearchDTO 재정의 절대 금지 (saved-search.ts 영역). | §3.1 API + §3.2 |
| **B.3** | `onday-app/src/lib/constants/saved-search-errors.ts` | `SAVED_SEARCH_ERROR_MAP: Record<SavedSearchErrorCode, Omit<SavedSearchErrorDTO, 'code' \| 'originalError'>>` 5 키 — 저장 1 (500) + 조회 1 (404) + 입력 검증 1 (400) + Geocoding 1 (200 best effort) + 세션 1 (401) | §3.4 |
| **B.4** | `onday-app/src/lib/helpers/saved-search-error.ts` | `createSavedSearchError(code, originalError?): SavedSearchErrorDTO` — SAVED_SEARCH_ERROR_MAP lookup + originalError 병합. Sentry catch 연계 가능 (REQ-NF-035). API-001/002/003 §3.6 createXError 패턴 4회째 일관 | (API-001/002/003 §3.6 패턴) |

### ★ Q3 (가) 2 파일 분리 strategy 표 (★ 본 ISSUE 핵심 분기 코드 입증, 명세 §3.1 분리 의도 + DB-006 §7 직접 충족 + DB-003 Q6 패턴 답습)

| 파일 | 영역 | 책임 | 정의 | 재정의 금지 |
|---|---|---|---|---|
| **`saved-search.ts`** | **DB-006 영역 base** | SearchParams JSONB 직렬화 대상 + Prisma row 의 TS DTO | SearchParams (mode: ServiceModeType + leisure 4 + filters 평행) + SavedSearchDTO (3 필드) | ★ **Request/Response/Error 정의 절대 금지** (책임 혼재 차단) |
| **`saved-search-api.ts`** | **API-005 영역** | API 통신 계약 (Request/Response) + 도메인 에러 | SaveSearchRequest (★ userId 필드 금지) + SaveSearchResponse + GetSavedSearchResponse + GeocodingValidationStatus + SavedSearchErrorCode 5종 + SavedSearchErrorDTO | ★ **SearchParams/SavedSearchDTO 재정의 절대 금지** (base 에서 import만, 책임 혼재 차단) |

**분리 strategy 근거 (3종)**:
1. **★ 명세 §3.1 분리 의도 직접 명시**: "`SaveSearchRequest`/`SaveSearchResponse`/`GetSavedSearchResponse` 가 `import type { SearchParams, SavedSearchDTO } from './saved-search'`" → 명세 자체가 base/API 분리 의도 명시.
2. **★ DB-006 §7 직접 명시**: "API-005 = `src/lib/types/saved-search.ts` 신규 작성 + `SearchParams` interface + `SavedSearchDTO` 정의 + `import { ServiceModeType } from './user'` 패턴 (DB-003 Q6 답습)" → base 파일 = saved-search.ts (단수형) 직접 명시.
3. **★ DB-003 Q6 패턴 답습**: diagnosis.ts (base) + diagnosis-errors.ts (API 영역 — 실제는 lib/constants/diagnosis-errors.ts) 분리 평행. base/API 책임 분리 입증 패턴.

**base → API import 검증 (Phase B.2 코드 L15)**:
```typescript
import type { SearchParams, SavedSearchDTO } from './saved-search';  // ★ base 에서 import (재정의 0)
```

### ★ (P1) 패턴 미묘 구분 표 (★ 3종 가이드 § 중 1번 — 본 ISSUE 4 회째 케이스, API-003 §2 § 답습)

(P1) "재사용 패턴" 은 **기존 도메인 entity 재사용** 정신 — Prisma 직접 노출 아님. base type 존재 여부에 따라 적용 분기:

| ISSUE | base type 존재 | (P1) 적용 | 패턴 | 신규 정의 |
|---|---|---|---|---|
| **API-001** | ✅ user.ts `AuthProviderType` (DB-002) | ✅ 적용 | `export type OAuthProvider = AuthProviderType;` (re-export) | OAuthSignInOptions / AuthSessionDTO 등 8 섹션 신규 |
| **API-002** | ✅ types.ts `CandidateArea` / `CommuteInfo` / `DiagnosisInput` (4/29 onday-app entity) + user.ts `ServiceModeType` + diagnosis.ts `DiagnosisStatusType` | ✅ 적용 (3 re-export) | `CandidateAreaDTO = CandidateArea` + 2종 (P1 re-export) | DiagnosisDTO / CreateDiagnosisResponse 등 4 신규 |
| **API-003** | ❌ **DB-004 base type 0** (DB-004 Q7/Q8 결정 — 코드 0 명세만, DTO=API-003 위임) | ❌ **적용 불가** | 자기 도메인 type 신규 정의 (Prisma model re-export 절대 금지) | ShareLinkMetaDTO / ReportDTO / Request/Response 등 6 섹션 전부 신규 |
| **★ API-005** | ❌ **DB-006 base type 0 — §7 로 정의 위임만** (DB-006 Q7/Q8 결정 — 코드 0 명세만, DTO=API-005 위임) | ❌ **적용 불가** | 자기 도메인 type 신규 정의 (Prisma model re-export 절대 금지) + ★ **base/API 2 파일 분리** | SearchParams (base) + SavedSearchDTO (base) + Request/Response/Geocoding/Error 등 신규 (API) |

**외부 도메인 type 재사용**:
- `ServiceModeType` from `'./user'` (★ Q2 (A) — DB-002 산출물 재사용 4 회째, DiagnosisMode 미사용 = Q2 가드)

**(P1) 정신 명시 (향후 가이드 — API-003 §2 일관)**:
- (P1) = 기존 도메인 entity 재사용 (`OAuthProvider=AuthProviderType` / `CandidateAreaDTO=CandidateArea`)
- (P1) ≠ Prisma 직접 노출 (`SavedSearchDTO=PrismaSavedSearch` 금지 — 관심사 분리)
- base type 부재 시 자기 도메인 신규 정의 = 정상 (DB-004→API-003 / DB-006→API-005 패턴 일관)

### ★ 분리/통합 strategy 표 (★ 3종 가이드 § 중 2번 — 본 ISSUE § 신규 owner, Wave 3+ 새 도메인 결정 매트릭스)

base 명시 / DB 영역 위임 / API 영역 위임에 따라 파일 분리 vs 통합 결정:

| ISSUE | DB §7 base 명시 | DB 영역 정의 | API 영역 | 결정 | 결과 |
|---|---|---|---|---|---|
| **API-002** | ✅ DB-003 §7 base (DiagnosisStatusType) | ✅ DB-003 정의 | diagnosis-errors.ts API | **diagnosis.ts (base) + diagnosis-errors.ts (API)** 분리 | DB-003 Q6 패턴 — base/API 분리 |
| **API-003** | ❌ DB-004 §7 base 0 (DTO 위임만) | — | share-link.ts 1 파일 통합 | **share-link.ts 1 파일 통합** (6 섹션) | base 0 → 자기 도메인 신규 통합 정상 |
| **★ API-005** | ✅ DB-006 §7 base 명시 (saved-search.ts neueru owner + SearchParams + SavedSearchDTO + ServiceModeType import + DB-003 Q6 답습 명시) | **DB-006 영역 = API-005 위임** (Q7/Q8 결정) | API 영역 = API-005 본 ISSUE | **★ saved-search.ts (DB-006 영역 base) + saved-search-api.ts (API-005 영역) 2 파일 분리** | DB-006 §7 직접 충족 + DB-003 Q6 패턴 답습 + 명세 §3.1 분리 의도 정합 |

**결정 매트릭스 (Wave 3+ 새 도메인 ISSUE 가이드)**:
- **"DB §7 base 명시 + DB 영역 정의 위임 = 2 파일 분리"** (예: API-005 본 ISSUE)
- **"DB §7 base 명시 + DB 영역 직접 정의 = 분리"** (예: API-002, DB-003 이미 정의)
- **"DB §7 base 0 + DTO 위임만 = 1 파일 통합"** (예: API-003, DB-004 base 0)

**부가 통찰 (본 ISSUE 특화)**:
- DB-006 = 코드 0 명세만 (Q7/Q8 DTO=API-005 위임) 인데도 §7 base 명시 → API-005 가 base 파일 신규 owner. "명세만 = 코드 0" 이 아닌 "정의 위임 = 명세 §7 매핑 직접 명시" 가 핵심.
- 향후 새 도메인 ISSUE: DB-* §7 base 명시 여부 + DB 영역 정의 owner 두 변수 기준으로 결정.

### ★ 도메인별 mapper 패턴 차이 표 (★ 3종 가이드 § 중 3번 — 본 ISSUE § 신규 owner, single/dual/single 3 패턴 비교)

API-001/002/003/005 의 mapper 인라인 패턴 비교:

| 도메인 | 인라인 위치 | 패턴 | 추출 owner | 결정 매트릭스 |
|---|---|---|---|---|
| **API-001** (Auth) | mock-auth M1~M4 인라인 | (mapper 적용 영역 외 — 세션 관리) | — | (mapper 영역 외) |
| **API-002** (Diagnosis) | N3 `app/api/diagnosis/[id]/route.ts:19-34` (single inline, Route only) | **single inline** | QRY-DIAG-001 single | Route only |
| **API-003** (ShareLink) | S3 `app/api/share/[uuid]/route.ts:12-54` (Route Handler) + S4 `app/share/[uuid]/page.tsx:15-?` (SSR) | **★ dual inline** (Route + SSR 양쪽) | QRY-SHARE-001 단일 owner (mapper 분리 금지) | Route + SSR 양쪽 |
| **★ API-005** (SavedSearch) | **SS1 `app/api/save/route.ts:49 findUnique + L55-61 인라인 mapper** (single inline, Route only) | **★ single inline (API-002 와 동일 패턴, API-003 dual 과 다름)** | QRY-SAVE-001 single | Route only |

**결정 매트릭스 (Wave 3+ 새 도메인 mapper 결정 가이드)**:
- **"Route only inline = single mapper owner"** (예: QRY-DIAG-001 / QRY-SAVE-001)
- **"Route + SSR dual inline = dual mapper 단일 owner (분리 금지)"** (예: QRY-SHARE-001)

**부가 통찰 (본 ISSUE 특화)**:
- ★ **SS1 = 단일 파일에 POST+GET+upsert+findUnique+인라인 mapper 통합** (66 lines) — API-005 mapper 가장 단순 (API-003 dual 보다, API-002 N3 single 과 동일 패턴).
- API-003 dual mapper = 신규 분기 패턴 발견 (Route + SSR 양쪽) / API-005 = API-002 single 패턴 재확인 (Route only).
- 3 패턴 비교 통해 새 도메인 분기 변수 명확 = "SSR 페이지 존재 여부" (UI 가 SSR 인지 클라이언트 컴포넌트 인지가 dual 분기 결정).

### ★ Q2 가드 사수 표 — DiagnosisMode 11 + DiagnosisStatus 2 호출처 + forward 0건 + ServiceModeType 4회 = 17 행 (★ Q2 가드 3회째 사수, API-002 9칸 첫 성공 / API-003 2회째 / 본 ISSUE 3회째 일관)

본 ISSUE Phase A 시점 grep 결과 (코드 호출처만, 주석 텍스트 제외):

| # | 위치 | 라인 | 역할 | 본 ISSUE | cleanup 통합 |
|---|---|---|---|---|---|
| DM1 | `src/lib/types.ts` | L2 | `export type DiagnosisMode` 정의 | 미수정 (가드) | 제거 |
| DM2 | `src/lib/types.ts` | L55 | `DiagnosisInput.mode: DiagnosisMode` | 미수정 (가드) | ServiceModeType |
| DM3 | `src/lib/types.ts` | L66 | `DiagnosisResult.mode: DiagnosisMode` | 미수정 (가드) | ServiceModeType |
| DM4 | `src/lib/types.ts` | L88 | `MockUser.mode: DiagnosisMode` | 미수정 (가드) | ServiceModeType (MOCK-005 동시) |
| DM5-7 | `src/features/diagnosis/mock-calculator.ts` | L5/85/193 | import + parameter ×2 | 미수정 (가드) | cleanup ISSUE |
| DM8-11 | `src/stores/diagnosis-store.ts` | L2/15/30/48 | import + state + action + initial | 미수정 (가드) | cleanup ISSUE |
| DS1 | `src/lib/types.ts` | L3 | `export type DiagnosisStatus` 정의 | 미수정 (가드) | 제거 |
| DS2 | `src/lib/types.ts` | L69 | `DiagnosisResult.status: DiagnosisStatus` | 미수정 (가드) | DiagnosisStatusType |
| **★ F0** | (SavedSearch 영역 forward 호출처) | **0건** | **DiagnosisMode/ServiceModeType direct import 0건 + forward 0건** — `saveSearchSchema = z.record(z.string(), z.unknown())` 통째 unknown | — | — |
| **★ ServiceModeType 4회 호출처** | (★ 재사용 4 회 일관 입증) | | | | |
| SMT1 | `src/lib/types/user.ts` | L5 | `export type ServiceModeType = 'couple' \| 'single'` (정의, DB-002) | 미수정 (가드) | — |
| SMT2 | `src/lib/types/diagnosis.ts` | L13 | `import type { ServiceModeType } from './user'` (API-002 산출물) | 미수정 (가드) | — |
| SMT3 | `src/lib/types/share-link.ts` | L11 | `import type { ServiceModeType } from './user'` (API-003 산출물) | 미수정 (가드) | — |
| **★ SMT4** | `src/lib/types/saved-search.ts` | **L13** (Phase B.1 신규) | **`import type { ServiceModeType } from './user'` (★ API-005 본 ISSUE 4 회째 일관)** | ✅ Phase B.1 신규 | — |

**합계: DiagnosisMode 11 + DiagnosisStatus 2 + forward 0 + ServiceModeType 4회 = 17 행** (코드 grep 검증).

**Q2 (A) ServiceModeType import-only 사유 (★ Q2 가드 3회째 사수, ★ 가장 약한 의존성 입증)**:
1. **★ 9칸+10칸+11칸 가드 보존 — 12칸 누적 (★ 11번째 회귀 0, API-002 첫 성공 + API-003 2회째 사수 + 본 ISSUE 3회째 사수)**: INFRA-001 / DB-001~007 / API-001/002/003 누적 11 ISSUE 동안 `types.ts` 무수정 가드 유지 + 본 ISSUE 11 번째 회귀 0 사수.
2. **★ forward 0건 (★ API-003 F1+F2 2건 보다 더 약함)**: `saveSearchSchema = z.record(z.string(), z.unknown())` 통째 unknown → SavedSearch 영역에 DiagnosisMode/ServiceModeType direct import 0건 + forward 0건. cleanup ISSUE 의존성 = **가장 약함** (어느 owner 채택해도 cleanup 영향 없음). SearchParams.mode = ServiceModeType 채택으로 cleanup ISSUE 자연 정합.
3. **★ ServiceModeType 재사용 4 회째 — 안정 패턴 입증**: user.ts 정의 + API-002/003/005 import 4회 일관 → 일회성 아닌 안정 패턴 입증. (P1) 정신 일관 (기존 도메인 entity 재사용).
4. **DB-006 Q3 / API-001 mock-auth M1~M4 / API-002 cleanup ISSUE 트리거 / API-003 cleanup ISSUE 트리거 일관**: 인라인 보존 + cleanup 별도 ISSUE 분리 패턴 답습.

### ★ SS1~SS3 SavedSearch 현실 코드 표 (위치/라인/역할/본 ISSUE 미수정/후행 통합 — DB-006 Q3 / API-002 N1~N6 / API-003 S1~S6 패턴 답습)

| # | 위치 | 라인 | 역할 | 본 ISSUE 처리 | 후행 통합 대상 |
|---|---|---|---|---|---|
| **SS1** | `onday-app/src/app/api/save/route.ts` | 66 (전체) | L5-7 `saveSearchSchema = z.object({ searchParams: z.record(z.string(), z.unknown()) })` ★ passthrough-like + L13 safeParse + L22-32 POST upsert + L49 findUnique + L55-61 인라인 mapper (saved.id/searchParams JSON.parse/savedAt.toISOString) — **single 파일 통합 (POST+GET+upsert+findUnique+mapper, ★ searchParams 내부 unknown — mode 검증 0)** | 미수정 | **CMD-SAVE-001** (Zod 추출 + ★ DB-006 §9.1 strict/passthrough 결정 + 호출처 통합 + bcrypt 미적용 — best effort) + **QRY-SAVE-001 single owner** (L49 findUnique + L55-61 인라인 mapper 추출, ★ single inline — API-002 N3 와 동일 패턴, API-003 dual 과 다름) |
| **SS2** | `onday-app/src/lib/db.ts` | 7 호출처 (L53/62/69/152/154/164/168) | mock savedSearch storage (upsert/findUnique) — in-memory fake (`savedSearchByUser: Map<string, SavedSearchRow>`) | 미수정 | **INFRA-002** (실 Prisma 전환 시 통합 — Postgres swap + `Json @db.JsonB` 전환 + RLS) |
| **SS3** | `onday-app/src/lib/validators/saved-search.ts` 부재 | — | DB-006 결정으로 코드 0 + 본 ISSUE Q3 (가) 2 파일 분리 신규 작성 시 = saved-search-api.ts (validators 신규 작성도 CMD-SAVE-001 영역) | 미수정 | **CMD-SAVE-001** (Zod 추출 + DB-006 §9.1 strict/passthrough 결정) |

본 ISSUE 가 SS1~SS3 추출 산출물을 미작성하는 사유 (DB-006 Q3 / API-001 Q6 / API-002 Q8/Q9 / API-003 Q7/Q8 일관 dead 회피):
- SS1 Zod: `api/save/route.ts:5-7` `z.record(z.string(), z.unknown())` passthrough-like 인라인 + Route Handler L13 호출처 활성 → 본 ISSUE 추출 시 SS1 우회 가드 위반 + ★ **DB-006 §9.1 strict/passthrough 결정 트리거 본 ISSUE 수신 → CMD-SAVE-001 위임으로 발신** (DB-006 §9.1 자체가 CMD-SAVE-001 위임 명시)
- SS1 mapper: L55-61 인라인 flat response → 본 ISSUE = DTO contract only, QRY-SAVE-001 single owner (★ API-002 N3 single 동일 패턴, API-003 S3+S4 dual 과 다름 — 위 mapper 패턴 차이 § 참조)
- SS2 mock storage: lib/db.ts 7 호출처 mock storage → INFRA-002 실 Prisma 전환 영역 (본 ISSUE 미수정, RLS 도 INFRA-002+DB-007 영역)
- SS3 validators 부재: DB-006 결정으로 코드 0 + CMD-SAVE-001 신규 작성 영역 (본 ISSUE 미수정)

### ★ 부재 산출물 표 (인라인 동작 중 / 영역 분리 → 위임 5+ + 명확화 2 = 7)

| 명세 §3 산출물 | 현실 (Phase A 확인) | 위임 대상 |
|---|---|---|
| §3.3 `lib/validators/saved-search-api.ts` `saveSearchRequestSchema` 신규 | SS1 `api/save/route.ts:5-7` `z.record(z.string(), z.unknown())` passthrough-like 인라인 동작 중 + Route Handler L13 호출처 활성 + **★ DB-006 §9.1 strict/passthrough 결정 트리거 본 ISSUE 수신** | **CMD-SAVE-001** (SS1 통합 + DB-006 §9.1 strict/passthrough 결정) |
| §3.5 `lib/mappers/saved-search-mapper.ts` `mapSavedSearchToDTO` + `mapToGetResponse` | **SS1 L49 findUnique + L55-61 인라인 mapper 동작 중** + `lib/mappers/` 빈 디렉토리 (API-001/002/003 시점 빈, 본 ISSUE 도 작성 안 함) | **★ QRY-SAVE-001 single owner** (★ SS1 single inline — API-002 N3 동일 패턴, API-003 dual 과 다름) |
| §3.6 `__tests__/types/saved-search-api.spec.ts` (4 케이스) | vitest config 부재 (DB-001~007 + API-001/002/003 일관) | **TEST-005 (저장 GWT)** |
| §3.7 `__tests__/validators/saved-search-api.spec.ts` (5 케이스) | 동상 | **TEST-005** |
| §3.8 `__tests__/mappers/saved-search-mapper.spec.ts` (3 케이스) | 동상 | **TEST-005 + TEST-007 (간이 시나리오) 분배** |
| **DB-006 §9.1 strict/passthrough 결정** | DB-006 §9.1 CMD-SAVE-001 위임 명시 + SS1 `z.record(z.string(), z.unknown())` passthrough 변형 인라인 동작 | **CMD-SAVE-001 영역 (본 ISSUE 미수정, §9 명확화만)** |
| **userId 서버 사이드 추출 (REQ-NF-021)** | SaveSearchRequest 에 userId 필드 금지 + Supabase Auth 세션 추출 | **CMD-SAVE-001 영역 (본 ISSUE = DTO contract only, B.2 주석으로만 명시)** |

### 선행 태스크 산출물

| 선행 Task ID | 제공 산출물 | 본 ISSUE 사용처 |
|---|---|---|
| **DB-002** (PR #76 머지 완료, main `d916a6f`) | `src/lib/types/user.ts` (`ServiceModeType` TS union, commit `2ea3a17`) | ★ B.1 `saved-search.ts:13` `import type { ServiceModeType } from './user'` — `SearchParams.mode` 에 사용 (Q2 (A) 핵심 — DiagnosisMode 미사용, ★ ServiceModeType 4 회째 재사용) |
| **DB-003** (PR #77 머지 완료) | `src/lib/types/diagnosis.ts` (Q6 패턴 — base + API 영역 분리 평행) | ★ **Q3 (가) 답습 기준** — diagnosis.ts (base) + diagnosis-errors.ts (API) 분리 = 본 ISSUE saved-search.ts + saved-search-api.ts 평행 |
| **DB-006** (PR #79 머지 완료, main `76a3b2a`) | `tasks/DB-006.md` (코드 0, 명세만 — Q7/Q8 결정으로 DTO=API-005 위임 + §7 saved-search.ts 신규 owner 명시 + §9.1 strict/passthrough = CMD-SAVE-001 위임) | ★ **본 ISSUE = DB-006 §7+§9.1 동시 트리거 수신**. §7 → saved-search.ts 신규 owner 직접 충족 + §9.1 → CMD-SAVE-001 위임 발신 |
| **API-001** (PR #81 머지 완료, main `ee83429`) | `lib/types/auth.ts` + `auth-errors.ts` + `auth-error.ts` + `tasks/API-001.md` (Q1~Q10 + L6 2차 정정 + (P1) OAuthProvider=AuthProviderType 패턴) | ★ 본 ISSUE 답습 기준 — (B) 절충 + Phase B 활성 + 커밋 2개 + (P1) 정신 |
| **API-002** (PR #82 머지 완료, main `891ae97`) | `lib/types/diagnosis.ts` (★ `CandidateAreaDTO = CandidateArea` re-export L27) + `diagnosis-errors.ts` + `diagnosis-error.ts` + `tasks/API-002.md` (Q1~Q10 + Q2 9칸 가드 첫 성공 + L6 3차 cleanup 트리거) | ★ 본 ISSUE 답습 기준 — Q2 첫 성공 + Phase B 패턴 + ★ mapper 패턴 차이 § 중 single (Route only) 분기 |
| **API-003** (PR #83 머지 완료, main `f0b012e`) | `lib/types/share-link.ts` (신규 첫 owner, 6 섹션 통합) + `share-link-errors.ts` + `share-link-error.ts` + `tasks/API-003.md` (★ (P1) 미묘 구분 § + S3+S4 dual mapper § + Wave 2 체인 작동 첫 코드 입증) | ★ 본 ISSUE 답습 기준 — (P1) 미묘 구분 답습 + ★ mapper 패턴 차이 § 중 dual 분기 비교 + ★ 분리/통합 strategy § 중 통합 분기 비교 |
| **DB-007** (PR #80 머지 완료) | L6 1차 발견 (추후 API-001 2차 / API-002 3차 정정) | 본 ISSUE = Q2 가드 3회째 사수 (cleanup ISSUE 의존성 가장 약함 — forward 0건) |
| DB-001 / DB-004 / DB-005 | 간접 선행 (Phase 패턴 + dead 회피 논리 답습 + DB-004→API-003 base 0 패턴 vs DB-006→API-005 base 명시 패턴 비교) | DB-* 패턴 답습 |

### 후행 ISSUE 정합성 (Q1~Q10 결정 트리거)

| 후행 Task ID | 본 ISSUE 위임 트리거 |
|---|---|
| **★ CMD-SAVE-001** (입력값 자동 저장) | SS1 (`saveSearchSchema = z.record(z.string(), z.unknown())` 인라인 + L13 safeParse 호출처 + L22-32 POST upsert) + ★ **DB-006 §9.1 strict/passthrough 결정** + bcrypt 미적용 (best effort) + B.2 `SaveSearchRequest` / `SaveSearchResponse` import + `SavedSearchErrorCode.SAVE_FAILED/INVALID_PARAMS/SESSION_REQUIRED` 활용 + ★ **userId 서버 사이드 추출 (REQ-NF-021)** (Supabase Auth 세션) |
| **★ QRY-SAVE-001** (저장된 조건 불러오기) — **single owner (★ API-003 dual 과 다름)** | ★ **SS1 L49 findUnique + L55-61 인라인 mapper 통합** (single inline). `mapSavedSearchToDTO(record: PrismaSavedSearch): SavedSearchDTO` + `mapToGetResponse(record, geocodingStatus?): GetSavedSearchResponse` 신규. B.2 `GetSavedSearchResponse` / B.1 `SavedSearchDTO` 활용 + **★ geocoding 재검증 시점 결정** (server-side vs client-side) + `SavedSearchErrorCode.NOT_FOUND/GEOCODING_FAILED` 활용 |
| **★ TEST-005** (저장 GWT) | type spec 4 + validator spec 5 + mapper spec 3 (정상 / NOT_FOUND / INVALID_PARAMS / GEOCODING_FAILED best effort) — AC-1/2/3/4 검증 |
| **★ TEST-007** (간이 시나리오) | mapper spec 3 케이스 일부 분배 + 통합 시나리오 (저장 → 복원 → geocoding 재검증 → "주소를 다시 입력해주세요" 안내) — REQ-FUNC-025 1초 복원 + REQ-NF-016 best effort |
| **★ INFRA-002** (Supabase Postgres) | SS2 `lib/db.ts` 7 호출처 mock storage → 실 Prisma 전환 + `searchParams Json @db.JsonB` 전환 + 인덱싱 평가 + RLS (DB-007 트리거) + AC-1~AC-4 활성 |
| **★ MOCK-005** (Mock 데이터) | B.1 활성 후 `SavedSearchDTO` Mock + `SearchParams` Mock (leisure 4 필드 포함) + geocodingStatus 시나리오 3종 |
| **UI-014** (저장된 조건 복원 UI) | B.2 `GetSavedSearchResponse` 활용 + 1초 복원 (REQ-FUNC-025) + geocoding 실패 안내 ("주소를 다시 입력해주세요") |
| **★ cleanup ISSUE (REFACTOR-L6 또는 MOCK-005 연계)** | API-002 §9.4 / API-003 cleanup 트리거 일관. 본 ISSUE 의존성 = **가장 약함** (forward 0건, types.ts 직접 import 0) |

---

## 3. 🛠️ Task Breakdown (실행 체크리스트)

> ★ API-001/002/003 패턴 답습 (Phase A 사전 검증 + Phase B 활성 4 파일 + Phase C 명세 동기화 + Phase D 커밋 2개) + (B) 절충 + (Q3 가) 2 파일 분리 + Q2 (A) 가드 3회째 사수.

- [x] **3.1 (base)** `lib/types/saved-search.ts` 신규 첫 owner (★ DB-006 영역 base) — **Phase B.1 완료**
  > **★ DB-006 §7 위임 트리거 직접 수신** — saved-search.ts 신규 owner + SearchParams + SavedSearchDTO + ServiceModeType import (DB-003 Q6 답습).
  > import 부 (정확 1, 정직 기록):
  > - `import type { ServiceModeType } from './user';` (Q2 (A) — DiagnosisMode 미사용, 9칸 가드 보존, ★ ServiceModeType 4 회째 재사용)
  > Prisma model import 0 + types.ts DiagnosisMode/DiagnosisStatus import 0
  > 2 섹션:
  > 1. SearchParams (mode: ServiceModeType + leisure 4 필드 + filters DiagnosisFilters 평행 — maxCommuteTime/budget 객체/timeRange enum/priorities)
  > 2. SavedSearchDTO (userId/searchParams/savedAt)
  > ★ Request/Response/Error 정의 절대 금지 (saved-search-api.ts 영역, 책임 혼재 차단)

- [x] **3.1 (API)** `lib/types/saved-search-api.ts` 신규 (★ API-005 영역) — **Phase B.2 완료**
  > **★ Q3 (가) 2 파일 분리 strategy 첫 코드 입증** — saved-search.ts (base) + saved-search-api.ts (API) 분리 (DB-003 Q6 패턴 답습).
  > import 부 (정확 1, 정직 기록):
  > - `import type { SearchParams, SavedSearchDTO } from './saved-search';` (base 에서 import, 재정의 0)
  > Prisma model import 0
  > 4 섹션:
  > 1. Request DTO: SaveSearchRequest (★ userId 필드 절대 금지, REQ-NF-021 — Supabase Auth 세션 서버 사이드 추출)
  > 2. Response DTO: SaveSearchResponse + GetSavedSearchResponse
  > 3. GeocodingValidationStatus (QRY-SAVE-001 영역 contract)
  > 4. SavedSearchErrorCode 5종 + SavedSearchErrorDTO

- [x] **3.2** `lib/types/saved-search-api.ts` `SavedSearchErrorCode` enum 5 종 — **Phase B.2 완료 (같은 파일 §4 섹션)**
  > 저장 1 (SAVE_FAILED) + 조회 1 (NOT_FOUND) + 입력 검증 1 (INVALID_PARAMS) + Geocoding 1 (GEOCODING_FAILED — best effort) + 세션 1 (SESSION_REQUIRED). API-001 9종 / API-002 8종 / API-003 7종 보다 적음 = **도메인 단순성 반영**.

- [⏸ CMD-SAVE-001 위임] **3.3** `lib/validators/saved-search-api.ts` `saveSearchRequestSchema` — **Q7 부재 산출물 결정**
  > **사유 5종 (DB-006 Q3 / API-001 Q6 / API-002 Q8 / API-003 Q7 일관 dead 회피 + 인라인 동작 중):**
  > 1. **★ SS1 인라인 동작 중 (Phase A.4 확인)**: `api/save/route.ts:5-7` `saveSearchSchema = z.object({ searchParams: z.record(z.string(), z.unknown()) })` passthrough-like 정상 동작 중.
  > 2. **★ Route Handler 호출처 활성**: `api/save/route.ts:13` `saveSearchSchema.safeParse(body)` 호출처 활성.
  > 3. **★ DB-006 §9.1 strict/passthrough 결정 트리거 본 ISSUE 수신**: DB-006 PR #79 §9.1 "Zod 스키마를 `z.object().passthrough()` 로 설정하여 하위 호환성을 유지할지, strict 모드로 갈지 **CMD-SAVE-001 작업 시 확정**" → 본 ISSUE 가 CMD-SAVE-001 §3.3 위임으로 받음.
  > 4. **CMD-SAVE-001 영역**: 06_TASK_LIST §2-A — 입력값 자동 저장 = saveSearchRequestSchema 의 owner. 본 ISSUE 가 추출 시 owner 침범 + SS1 우회 가드 위반.
  > 5. **★ userId 서버 사이드 추출 통합 트리거**: REQ-NF-021 + B.2 `SaveSearchRequest` 정의로 명시 + CMD-SAVE-001 시점 Supabase Auth 세션 추출 통합.

- [x] **3.4** `lib/constants/saved-search-errors.ts` `SAVED_SEARCH_ERROR_MAP` (5 키) — **Phase B.3 완료**
  > 5 키 (SavedSearchErrorCode enum 전수) × `{message: string (한국어), httpStatus: number}` 매핑. Omit pattern 4 회째. HTTP 상태: 500 (SAVE_FAILED) / 404 (NOT_FOUND) / 400 (INVALID_PARAMS) / **★ 200 (GEOCODING_FAILED — best effort, REQ-NF-016 일관)** / 401 (SESSION_REQUIRED).

- [⏸ QRY-SAVE-001 single owner 위임] **3.5** `lib/mappers/saved-search-mapper.ts` `mapSavedSearchToDTO` + `mapToGetResponse` — **Q8 부재 산출물 결정**
  > **사유 5종 (DB-006 Q3 / API-002 Q9 / API-003 Q8 일관 dead 회피 + ★ mapper 패턴 차이 명시):**
  > 1. **★ SS1 single inline 동작 중 (Phase A.4 확인)**: `api/save/route.ts:49` findUnique + L55-61 인라인 flat mapper (saved.id/searchParams JSON.parse/savedAt.toISOString) — single 파일 통합.
  > 2. **★ lib/mappers/ 빈 디렉토리**: API-001/002/003 시점 빈 디렉토리 유지 + 본 ISSUE 도 작성 안 함 → mkdir + 단일 파일 만들 시 dead + SS1 우회.
  > 3. **★ QRY-SAVE-001 single owner 영역**: 06_TASK_LIST §2-A — 저장된 조건 불러오기 = mapSavedSearchToDTO + mapToGetResponse owner. 본 ISSUE 추출 시 owner 침범 + SS1 우회 가드 위반.
  > 4. **★ mapper 패턴 차이 명시**: SS1 single inline = QRY-SAVE-001 single owner (★ API-002 N3 single 동일 패턴, **★ API-003 S3+S4 dual 과 다름** — 위 §2 mapper 패턴 차이 § 참조).
  > 5. **Prisma model 의존**: mapper 시그니처 = `(record: PrismaSavedSearch) => SavedSearchDTO` — Prisma Client import 필요. 본 ISSUE = DTO only owner → Prisma import 회피 (관심사 분리, API-001/002/003 일관).

- [x] **3.6 (helper)** `lib/helpers/saved-search-error.ts` `createSavedSearchError(code, originalError?)` — **Phase B.4 완료** ★ 명세 v1.0 §3.6 spec 항목 대비 추가 helper (API-001/002/003 §3.6 패턴 4 회째 일관)
  > `(code: SavedSearchErrorCode, originalError?: string): SavedSearchErrorDTO` — SAVED_SEARCH_ERROR_MAP lookup + originalError 병합. Sentry catch 연계 가능 (REQ-NF-035).

- [⏸ TEST-005 위임] **3.6 (spec)** `__tests__/types/saved-search-api.spec.ts` (4 케이스)
  > **사유 3종**: vitest config 부재 (Phase A.7 확인) → spec 작성해도 실행 불가 / AC-1/2/3/4 CMD-SAVE / QRY-SAVE 위임 → spec 동일 시점 / TEST-005 영역 (저장 GWT).

- [⏸ TEST-005 위임] **3.7** `__tests__/validators/saved-search-api.spec.ts` (5 케이스: 정상 / searchParams 누락 / mode 무효 / 빈 객체 / DB-006 §9.1 passthrough vs strict 비교)
  > 사유: §3.3 Zod 자체 위임 (CMD-SAVE-001) → spec 동시 위임. DB-006 §9.1 결정에 따라 케이스 분기.

- [⏸ TEST-005 + TEST-007 분배 위임] **3.8** `__tests__/mappers/saved-search-mapper.spec.ts` (3 케이스: 정상 / null record / geocoding 실패) + **★ TEST-007 간이 시나리오 케이스 추가** (저장 → 복원 → geocoding 재검증 → "주소를 다시 입력해주세요" 안내)
  > 사유: §3.5 mapper 자체 위임 (QRY-SAVE-001) → spec 동시 위임. 간이 시나리오 = TEST-007 분배.

- [본 ISSUE 미수정, §9 명확화만] **3.9 (Q10)** DB-006 §9.1 strict/passthrough 결정 — **CMD-SAVE-001 영역**
  > DB-006 §9.1 CMD-SAVE-001 위임 명시 + SS1 `z.record(z.string(), z.unknown())` passthrough 변형 인라인 동작 중. 본 ISSUE = DTO contract only, Zod 결정 owner 아님 — §9 명확화만.

- [본 ISSUE 미수정, §9 명확화만] **3.10 (보조)** userId 서버 사이드 추출 (REQ-NF-021) — **CMD-SAVE-001 영역**
  > B.2 `SaveSearchRequest` 에 userId 필드 절대 금지 (주석으로만 명시) + Supabase Auth 세션 서버 사이드 추출. 본 ISSUE = DTO contract only.

### ★ Q1~Q10 결정 vs 명세 v1.0 mismatch 추적 표 (API-001/002/003 패턴 답습)

| Q | 영역 | 명세 v1.0 (API-005.md) | 현실 (onday-app) | 결정 | 후행 처리 |
|---|---|---|---|---|---|
| **Q1** ★ | 작업 모드 (가장 상위, API-001/002/003 답습) | §3.1~3.8 8개 산출물 신규 작성 | DB-006 산출물 = 코드 0 (Q7/Q8 DTO=API-005 위임) + SS1 Zod 인라인 passthrough-like + SS1 mapper 인라인 single + spec 미실행 가능 + DB-006 §9.1 결정 미확정 | (B) **절충 — API-001/002/003 패턴 정확 답습 + Phase B 활성 (★ 4 파일, API-003 +1) + 커밋 2개 (feat+docs 분리)** | Phase B 작성 (B.1/B.2/B.3/B.4) + Phase D 커밋 2개 |
| **페이스** | 세션 진행 범위 | — | API-003 풀세트 직후 + Q3 새 큰 분기 (2 파일 분리) + SS1~SS3 + Wave 2 트랙 D 완성 마일스톤 | (가) **grill + 메모리까지 — Phase A~D 다음 세션** (3 연속 풀세트 검수 위험 분기 답습) | 본 세션 grill 완료, Phase 다음 세션 |
| **Q2** | SearchParams.mode owner (가드 충돌 점검) | (명세 §3.1 SearchParams.mode 'couple' \| 'single' literal) | ★ **forward 0건** (★ API-003 F1+F2 2건 보다 더 약함) — `saveSearchSchema = z.record(z.string(), z.unknown())` 통째 unknown + types.ts DiagnosisMode 직접 import 0 + ServiceModeType 재사용 4 회째 안정 패턴 | **(A) ServiceModeType import** (`from './user'`) — Q3 strategy 에 포섭. Q2 가드 3회째 사수 (API-002 첫 성공 / API-003 2회째 / 본 ISSUE 3회째 일관) | cleanup ISSUE 자연 정합 (★ 의존성 가장 약함 — forward 0건) |
| **Q3** ★★ | saved-search.ts 신규 정의 strategy (본 ISSUE 핵심 분기) | §3.1 base + API DTO 같은 파일 또는 분리 미명시 | DB-006 §7 base 명시 (saved-search.ts 신규 owner + SearchParams + SavedSearchDTO + ServiceModeType import + DB-003 Q6 답습 명시) + DB-006 §9.1 strict/passthrough 결정 위임 + Prisma model SavedSearch 4 필드 | **(가) 2 파일 분리** — ① `saved-search.ts` (DB-006 영역 base) ② `saved-search-api.ts` (API-005 영역) — ★ **DB-006 §7 직접 충족 + DB-003 Q6 패턴 답습 + 명세 §3.1 분리 의도 정합 + ★ 분리/통합 strategy § 신규 owner** | 후행 import 활용 (CMD-SAVE / QRY-SAVE / TEST-005·007 / INFRA-002 / MOCK-005 / UI-014) |
| **Q4** | §3.2 SavedSearchErrorCode enum 5 종 | 신규 작성 | 부재 (Route Handler `console.error` + 인라인 한국어 메시지) | (A) **✅ Phase B.2 작성** — API-001/002/003 §3.2 패턴 4 회째 일관 + 카테고리 (저장 1 / 조회 1 / 입력 검증 1 / Geocoding 1 / 세션 1) — **★ 도메인 단순성 반영 (API-001 9 / API-002 8 / API-003 7 보다 적음)** | 후행 CMD-SAVE / QRY-SAVE import 활용 |
| **Q5** | §3.4 SAVED_SEARCH_ERROR_MAP 5 키 | 신규 작성 | 부재 + `lib/constants/` (auth-errors.ts + diagnosis-errors.ts + share-link-errors.ts 존재) | (A) **✅ Phase B.3 작성** — API-001/002/003 패턴 4 회째 일관. HTTP 상태 500/404/400/200(★ best effort)/401. Omit pattern 4 회째 | 후행 createSavedSearchError lookup 대상 |
| **Q6** (추가) | createSavedSearchError helper | (명세 v1.0 부재 — API-001/002/003 §3.6 패턴 추가) | 부재 + `lib/helpers/` (auth-error.ts + diagnosis-error.ts + share-link-error.ts 존재) | (A) **✅ Phase B.4 작성** — API-001/002/003 §3.6 createXError 패턴 4 회째 일관 | 후행 CMD-SAVE / QRY-SAVE import (SavedSearchErrorDTO 생성) |
| **Q7** | §3.3 saveSearchRequestSchema Zod | 신규 작성 | SS1 `api/save/route.ts:5-7` `z.record(z.string(), z.unknown())` passthrough-like 인라인 + Route Handler 호출처 활성 + **★ DB-006 §9.1 strict/passthrough 결정 트리거 본 ISSUE 수신** | (A) **⏸ CMD-SAVE-001 위임** — SS1 인라인 보존 (DB-006 Q3 / API-002 Q8 / API-003 Q7 패턴) + DB-006 §9.1 위임 발신 | CMD-SAVE-001 (§9.1) |
| **Q8** | §3.5 mapSavedSearchToDTO + mapToGetResponse | 신규 작성 | **★ SS1 L49 findUnique + L55-61 인라인 mapper single inline** (Route only) + `lib/mappers/` 빈 디렉토리 | (A) **⏸ QRY-SAVE-001 single owner** — SS1 single inline 패턴 (★ API-002 N3 single 동일, ★ API-003 dual 과 다름) + ★ **mapper 패턴 차이 § 신규 owner** + API-001 Q6 / API-002 Q9 / API-003 Q8 dead 회피 패턴 | QRY-SAVE-001 (§9.2) |
| **Q9** | §3.6/3.7/3.8 spec ×3 | type spec 4 + validator spec 5 + mapper spec 3 | vitest config 부재 + AC CMD-SAVE/QRY-SAVE 위임 | (A) **⏸ TEST-005 (저장 GWT) + TEST-007 (간이 시나리오) 분배 위임** — DB-001~007 + API-001/002/003 일관 | TEST-005 + TEST-007 (§9.3) |
| **Q10** | DB-006 §9.1 strict/passthrough 결정 명확화 | DB-006 §9.1 CMD-SAVE-001 위임 명시 | DB-006 결정 그대로 + SS1 passthrough-like 인라인 등가 동작 중 | **본 ISSUE 미수정, §9 명확화만** — CMD-SAVE-001 영역, DB-006 §9.1 정합. 본 ISSUE = DTO contract only | CMD-SAVE-001 (§9.4) |
| **보조 1** | userId 서버 사이드 추출 (REQ-NF-021) | (명세 §3.1 SaveSearchRequest userId 필드 미명시 — 모호) | SS1 `userId = "mock-user-001"` 인라인 (mock 모드) | **본 ISSUE = SaveSearchRequest userId 필드 절대 금지 + B.2 주석 명시. CMD-SAVE-001 위임** | CMD-SAVE-001 (§9.5) |
| **보조 2** | SearchParams 스키마 진화 / geocoding 재검증 시점 / DiagnosisFilters 일관 | 명세 §9.1~9.3 보존 | 부재 (CMD-SAVE/QRY-SAVE 영역) | §9 follow-up 기록 | CMD-SAVE / QRY-SAVE / DB-006 §9.1 동기화 (§9.6/9.7) |

---

## 4. ✅ Acceptance Criteria (GWT 패턴, BDD — API-001/002/003 ⏸ 패턴 일관 + Phase B 작성분 충족)

**AC-1 (정상, ⏸ CMD-SAVE-001 위임):** saveSearch Request Zod 검증 통과
- **Given** 유효한 `searchParams` (서울 강남구 주소, `maxCommuteTime: 60`, `mode: 'couple'`) 포함 요청
- **When** `saveSearchRequestSchema.parse(request)` 호출 (CMD-SAVE-001 시점, 현재 SS1 `z.record(z.string(), z.unknown())` 인라인 동작 중)
- **Then** 검증 통과, 파싱된 객체의 `searchParams.mode` 가 `'couple'`
- **Status:** ⏸ CMD-SAVE-001 시점 정합 검증 (SS1 통합 + ★ DB-006 §9.1 strict/passthrough 결정 + 명세 §3.3 → 현실 saveSearchSchema 차이 정리). ★ 본 ISSUE B.2 `SaveSearchRequest` 신규 정의 완료 — Zod schema z.infer 타입 정합 확인은 CMD-SAVE-001 시점.

**AC-2 (예외, ⏸ CMD-SAVE-001 위임):** searchParams 누락 시 Zod 에러
- **Given** `searchParams` 필드가 없는 요청 객체
- **When** `saveSearchRequestSchema.parse(request)` 호출
- **Then** ZodError 발생, `issues[0].path` 가 `['searchParams']`
- **Status:** ⏸ CMD-SAVE-001 시점 검증 (SS1 인라인 `safeParse` 정합).

**AC-3 (예외, ⏸ QRY-SAVE-001 single owner 위임):** 저장된 기록 없음 시 응답 구조 검증
- **Given** DB 에 해당 `userId` 의 SavedSearch 레코드가 없는 상태
- **When** `mapToGetResponse(null)` 호출 (QRY-SAVE-001 시점, 현재 SS1 L51-53 인라인 `{ saved: null }` 동작 중)
- **Then** `found === false`, `savedSearch === null`, `geocodingStatus === undefined`
- **Status:** ⏸ QRY-SAVE-001 시점 검증. ★ 본 ISSUE B.2 `GetSavedSearchResponse.found` + `savedSearch: SavedSearchDTO | null` 정의 완료 — **1:0..1 (found false 정상 케이스 vs NOT_FOUND 에러 구분)**.

**AC-4 (경계, ⏸ QRY-SAVE-001 위임):** geocoding 실패 시 안내 메시지 포함
- **Given** SavedSearch 레코드 존재하나 저장된 주소A 의 geocoding 이 실패
- **When** `mapToGetResponse(record, { addressAValid: false, addressBValid: true })` 호출
- **Then** `geocodingStatus.addressAValid === false`, `geocodingStatus.message === '주소를 다시 입력해주세요'` (REQ-FUNC-025)
- **Status:** ⏸ QRY-SAVE-001 시점 검증. ★ 본 ISSUE B.2 `GeocodingValidationStatus` 정의 완료.

**AC-5 (정상, ✅ Phase B 산출물 충족):** SavedSearchErrorCode 전수 매핑 검증 (API-001 AC-5 / API-002 AC-3 / API-003 AC-5 패턴 4 회째)
- **Given** `SavedSearchErrorCode` enum 의 5 값 모두
- **When** `SAVED_SEARCH_ERROR_MAP[code]` 조회
- **Then** 5 값 모두 매핑 존재, `message` 한국어 문자열, `httpStatus` 100~599 범위 (★ GEOCODING_FAILED 는 200 best effort)
- **Status:** ✅ Phase B.3 `SAVED_SEARCH_ERROR_MAP` 5 키 전수 매핑 작성 완료 — **TS 타입 레벨 `Record<SavedSearchErrorCode, ...>` 강제로 누락 시 컴파일 에러** (tsc Phase B.5 exit 0 검증).

**AC-6 (정상, ✅ Phase B 산출물 충족):** Q3 (가) 2 파일 분리 책임 분리 검증 (★ 본 ISSUE 특화)
- **Given** Phase B.1 `saved-search.ts` (DB-006 영역 base) + Phase B.2 `saved-search-api.ts` (API-005 영역)
- **When** `grep "interface SearchParams\|interface SavedSearchDTO" saved-search-api.ts` + `grep "interface SaveSearchRequest\|interface SaveSearchResponse" saved-search.ts` 검증
- **Then** 양쪽 0 매치 (base 에 Request/Response 0 + API 에 SearchParams/SavedSearchDTO 재정의 0) + saved-search-api.ts L15 `import type { SearchParams, SavedSearchDTO } from './saved-search'` 확인
- **Status:** ✅ Phase B.5 grep 검증 완료 — **책임 분리 가드 첫 코드 입증** + DB-003 Q6 패턴 답습 + 명세 §3.1 분리 의도 정합.

---

## 5. ⚙️ Non-Functional Constraints (NFR 강제 인용)

| NFR ID | SRS 본문 인용 | 본 태스크에서의 검증 방법 |
|---|---|---|
| REQ-NF-016 | "입력값 자동 저장 성공률 — Best effort (저장 실패 시 무시, 사용자 미통지)" (§4.2.2) | ✅ B.2 `SaveSearchResponse` 에 `success: boolean` + optional `error?: SavedSearchErrorCode` 구조 + B.3 `GEOCODING_FAILED` HTTP 200 (best effort) — 실패 시 UI 가 에러를 노출하지 않는 패턴이 타입 레벨에서 지원 |
| REQ-NF-021 | "비인가 접근 시 개인정보 노출 0건" (§4.2.3) | ✅ B.2 `SaveSearchRequest` 에 `userId` 필드 절대 금지 + 주석 "Supabase Auth 세션 서버 사이드 추출 (클라이언트 전달 금지)" 명시. ⏸ TEST-005 시점 통합 테스트 (CMD-SAVE-001 서버 사이드 추출 검증) |
| REQ-NF-035 | "에러 로그 알림 — Sentry 기본 알림 설정 사용" (§4.2.6) | ✅ B.2 `SavedSearchErrorDTO.originalError` 필드 정의 + B.4 `createSavedSearchError()` 가 originalError 받아 Sentry catch 연계 가능. ⏸ CMD-SAVE / QRY-SAVE (실 Sentry.captureException) + MON-001 (Sentry 통합) |
| REQ-FUNC-025 | "다음 방문 시 복원 시간은 1초 이내 + geocoding 실패 시 '주소를 다시 입력해주세요' 안내" (§4.1.5) | ✅ B.2 `GetSavedSearchResponse.geocodingStatus.message?` optional + ⏸ QRY-SAVE-001 시점 1초 복원 + 안내 메시지 활성 |

---

## 6. 📦 Deliverables (산출물 명시 — 신규/보존/위임/정직성 4 구역)

### ✅ 신규 (5 — 4 코드 + 1 docs, Phase B + C, ★ API-003 +1)

**Phase B 코드 (4 파일)**:
- `onday-app/src/lib/types/saved-search.ts` (신규, **★ DB-006 영역 base 첫 owner**) — SearchParams (mode: ServiceModeType + leisure 4 + filters DiagnosisFilters 평행) + SavedSearchDTO (3 필드). import 부 정확 1 (`ServiceModeType from './user'`)
- `onday-app/src/lib/types/saved-search-api.ts` (신규, **★ API-005 영역**) — Request 1 (★ userId 필드 금지) + Response 2 + Geocoding + SavedSearchErrorCode 5종 + ErrorDTO. import 부 정확 1 (`SearchParams + SavedSearchDTO from './saved-search'`)
- `onday-app/src/lib/constants/saved-search-errors.ts` — SAVED_SEARCH_ERROR_MAP 5 키 (HTTP 500/404/400/200/401)
- `onday-app/src/lib/helpers/saved-search-error.ts` — createSavedSearchError (API-001/002/003 §3.6 패턴 4 회째)

**Phase C 명세 (1 파일)**:
- `tasks/API-005.md` — 본 명세 현실 동기화 (Q1~Q10 + Q3 (가) 2 파일 분리 strategy § + 3종 가이드 § 완성 + Q2 가드 사수 17행 + SS1~SS3 + ★ 도메인별 mapper 패턴 차이 § + Wave 2 트랙 D 완성 마일스톤 § + DB-006 §7+§9.1 동시 트리거 수신 § + Wave 2 체인 작동 4회째 § + §9 follow-up 7+)

### ✅ 보존 (DB-001~007 12 + API-001/002/003 산출물 9 + types.ts + 11+2+ServiceModeType 4회 호출처 + SS1~SS3 + S1~S6 + N1~N6 + mock-auth M1~M4 + .env.example = 25+종, 본 ISSUE 절대 미수정)

**DB-001~007 누적 가드 (12)**: `prisma/schema.prisma`, `prisma.config.ts`, `prisma/migrations/`, `prisma/seed.ts`, `src/lib/db.ts`, `src/lib/types/errors/.gitkeep`, **`src/lib/types/user.ts`** (DB-002 — B.1 `ServiceModeType` import 재사용 4 회째), `src/lib/types/diagnosis.ts` (DB-003 + API-002 산출물), `__tests__/db/.gitkeep`, `package.json`, design 루트, `06_TASK_LIST_v1.3.md`

**API-001 산출물 (3)**: `lib/types/auth.ts` + `auth-errors.ts` + `auth-error.ts` — 본 ISSUE 미수정

**API-002 산출물 (3)**: `lib/types/diagnosis.ts` (DB-003 보존 + append) + `diagnosis-errors.ts` + `diagnosis-error.ts` — 본 ISSUE 미수정

**API-003 산출물 (3)**: `lib/types/share-link.ts` (신규 첫 owner) + `share-link-errors.ts` + `share-link-error.ts` — 본 ISSUE 미수정

**Q2 가드 (17 행, ★ 3회째 사수)**: `src/lib/types.ts` (DiagnosisMode 11 호출처 DM1~DM11 + DiagnosisStatus 2 호출처 DS1~DS2 + forward 0건 F0 + ServiceModeType 4 회 호출처 SMT1~SMT4) — 위 §2 Q2 가드 사수 표 참조

**SS1~SS3 SavedSearch 현실 코드 가드 (DB-006 Q3 / API-002 N1~N6 / API-003 S1~S6 패턴 답습)**: SS1 `api/save/route.ts` 66 lines / SS2 `lib/db.ts` 7 호출처 / SS3 `validators/saved-search.ts` 부재 — 본 ISSUE 미수정

**S1~S6 + N1~N6 + mock-auth M1~M4 (API-001/002/003 가드 누적)**: 본 ISSUE 무관여

**`.env.example` Supabase 3종**: 본 ISSUE 미수정

### ⏸ 위임 (5 + 명확화 2 = 7)

- §3.3 `lib/validators/saved-search-api.ts` `saveSearchRequestSchema` (현실 SS1 통합 + DB-006 §9.1 strict/passthrough 결정 수신) → **CMD-SAVE-001** (Q7)
- §3.5 `lib/mappers/saved-search-mapper.ts` `mapSavedSearchToDTO` + `mapToGetResponse` (★ SS1 single inline 통합) → **QRY-SAVE-001 single owner (★ API-002 N3 동일, API-003 dual 과 다름)** (Q8)
- §3.6 type spec 4 케이스 + §3.7 validator spec 5 케이스 → **TEST-005 (저장 GWT)** (Q9)
- §3.8 mapper spec 3 케이스 + 간이 시나리오 → **TEST-005 + TEST-007 분배** (Q9)
- SS2 `lib/db.ts` 7 호출처 mock storage → **INFRA-002** (실 Prisma 전환 + Postgres `Json @db.JsonB`)
- **DB-006 §9.1 strict/passthrough 결정 → CMD-SAVE-001 명확화** (Q10, DB-006 §9.1 정합)
- **userId 서버 사이드 추출 (REQ-NF-021) → CMD-SAVE-001 명확화** (보조 1, B.2 주석으로만 명시)

### 🔄 정직성 (1 — ★ Wave 2 트랙 D 완성 + 3종 가이드 § 완성)

- **★ Wave 2 트랙 D 완성 마일스톤** — API-001 PR #81 (Wave 2 첫, Phase B 부활) → API-002 PR #82 (Q2 9칸 가드 코드 사수 첫 성공 + L6 3차 cleanup 트리거) → API-003 PR #83 (Wave 2 체인 작동 첫 코드 입증 + (P1) 미묘 구분 § + dual mapper §) → **API-005 본 ISSUE (Q3 (가) 분리 strategy § + mapper 패턴 차이 § + 3종 가이드 § 완성 + Wave 2 트랙 D 완성)**. 본 ISSUE 머지 시 Critical Path Wave 3 진입 (MOCK / TEST / CMD / QRY 트랙). §9 본문 § 참조.

---

## 7. 🔗 Dependencies (의존성 — 양방향)

### 선행 (이 ISSUE 시작 전 필요)

- **DB-002** (PR #76 머지 완료): `user.ts` `ServiceModeType` — ★ B.1 `import type { ServiceModeType } from './user'` 재사용 (Q2 (A) 핵심, ★ 4 회째).
- **DB-003** (PR #77 머지 완료): `diagnosis.ts` (★ Q6 base + API 영역 분리 평행 패턴 답습 기준).
- **DB-006** (PR #79 머지 완료, main `76a3b2a`): `tasks/DB-006.md` (코드 0, 명세만 — Q7/Q8 DTO=API-005 위임 + §7 saved-search.ts 신규 owner 명시 + §9.1 strict/passthrough=CMD-SAVE-001 위임). ★ **본 ISSUE = DB-006 §7+§9.1 동시 트리거 수신** (§7 직접 충족 + §9.1 위임 발신).
- **API-001** (PR #81 머지 완료, main `ee83429`): API Contract 트랙 (B) 절충 + 커밋 2개 + (P1) OAuthProvider=AuthProviderType 패턴. ★ 본 ISSUE 답습 기준.
- **API-002** (PR #82 머지 완료, main `891ae97`): `lib/types/diagnosis.ts` (★ Q2 9칸 가드 첫 성공 + L6 3차 cleanup 트리거 + ★ N3 single mapper 패턴). ★ 본 ISSUE 답습 기준 — Q2 + mapper single 분기.
- **API-003** (PR #83 머지 완료, main `f0b012e`): `lib/types/share-link.ts` (★ (P1) 미묘 구분 § + S3+S4 dual mapper § + Wave 2 체인 작동 첫 코드 입증). ★ 본 ISSUE 답습 기준 — (P1) 미묘 구분 + mapper dual 분기 비교 + 분리/통합 strategy 통합 분기 비교.
- **DB-007** (PR #80 머지 완료): L6 1차 발견 — 본 ISSUE = Q2 가드 3회째 사수 (cleanup 의존성 가장 약함).
- **DB-001 / DB-004 / DB-005**: 간접 선행 (Phase 패턴 + dead 회피 논리 답습 + DB-004→API-003 base 0 패턴 vs DB-006→API-005 base 명시 패턴 비교 — 분리/통합 strategy 결정 매트릭스 근거).

### 후행 (이 ISSUE 완료 후 차례로 가능)

- **★ CMD-SAVE-001** (입력값 자동 저장): SS1 (saveSearchSchema + L13 safeParse + L22-32 POST upsert 추출) + ★ **DB-006 §9.1 strict/passthrough 결정** + ★ **userId 서버 사이드 추출 (REQ-NF-021)** + B.2 import + best effort 패턴
- **★ QRY-SAVE-001** (저장된 조건 불러오기) — **single owner (★ API-003 dual 과 다름)**: ★ SS1 L49 findUnique + L55-61 인라인 mapper 통합 (single inline, ★ API-002 N3 동일 패턴) + B.2 + B.1 활용 + ★ **geocoding 재검증 시점 결정** + REQ-FUNC-025 1초 복원
- **★ TEST-005** (저장 GWT): 정상 / NOT_FOUND / INVALID_PARAMS / GEOCODING_FAILED best effort (AC-1~5)
- **★ TEST-007** (간이 시나리오): 저장 → 복원 → geocoding 재검증 → "주소를 다시 입력해주세요" 안내 (REQ-FUNC-025 1초 + REQ-NF-016 best effort 통합)
- **★ INFRA-002** (Supabase Postgres): SS2 lib/db.ts 7 호출처 mock storage → 실 Prisma 전환 + `searchParams Json @db.JsonB` + RLS (DB-007 트리거)
- **★ MOCK-005** (Mock 데이터): B.1 활성 후 SavedSearchDTO + SearchParams Mock (leisure 4 필드 포함) + geocodingStatus 시나리오 3종
- **UI-014** (저장된 조건 복원 UI): B.2 GetSavedSearchResponse 활용 + 1초 복원 + geocoding 실패 안내
- **★ cleanup ISSUE (REFACTOR-L6 또는 MOCK-005 연계)**: API-002 §9.4 / API-003 cleanup 트리거 일관. 본 ISSUE 의존성 = **가장 약함** (forward 0건, types.ts 직접 import 0).

---

## 8. 🧪 Test Plan (검증 절차)

### Phase A (본 ISSUE) — 사전 검증 (read-only) — ✅ 완료

- ✅ A.1 main 동기화: `891ae97..f0b012e` fast-forward (PR #83 머지본 흡수 — API-003 산출물 3 신규)
- ✅ A.2 DB-001~007 + API-001/002/003 산출물 main 반영 확인 (auth/diagnosis/share-link.ts + 각 errors.ts + 각 error.ts = 9 파일)
- ✅ A.3 `types.ts:1-3` (가드) + `user.ts` (ServiceModeType) + `diagnosis.ts` / `auth.ts` / `share-link.ts` (산출물) read-only
- ✅ A.4 **SS1~SS3 SavedSearch 현실 코드 grep** — SS1 `api/save/route.ts` 66 lines (L5-7 z.record + L13 safeParse + L22-32 POST + L49 findUnique + L55-61 인라인 mapper) / SS2 `lib/db.ts` 7 호출처 (L53/62/69/152/154/164/168) / SS3 `validators/saved-search.ts` 부재 (메모리 정확 일치)
- ✅ A.5 **★ Q2 DiagnosisMode/ServiceModeType forward grep** — SavedSearch 영역 direct import 0건 + forward 0건 (★ API-003 F1+F2 2건 보다 더 약함, ★ 가장 약한 의존성 입증)
- ✅ A.6 Prisma model SavedSearch read-only (4 필드 + 1 relation `user User @relation onDelete: Cascade` — Q8 mapper sig 근거, 본 ISSUE 미작성) + DB-006 §7 + §9.1 트리거 확인
- ✅ A.7 vitest config 부재 (DB-001~007 + API-001/002/003 일관)
- ✅ A.8 `lib/constants/` + `lib/helpers/` (각 3 파일 = API-001/002/003 산출물) + `lib/types/` (auth.ts + diagnosis.ts + share-link.ts + user.ts + errors/) — saved-search* 4 신규 작성 위치 OK
- ✅ A.9 `prisma validate` exit 0 + `prisma generate` exit 0 (7.8.0, 20ms) + `tsc --noEmit` exit 0
- ✅ A.10 env-less `npm run build` exit 0 + **Middleware 32.5 kB** (INFRA-001 AC-4 anchor, ★ **11번째 회귀 0**)
- ✅ A.11 `feature/API-005` 브랜치 분기 + untracked 2건 외 변경 0

### Phase B (본 ISSUE) — 활성, 4 파일 신규 — ✅ 완료

- ✅ B.1 `lib/types/saved-search.ts` 신규 (★ DB-006 영역 base, SearchParams + SavedSearchDTO, ServiceModeType import 4 회째)
- ✅ B.2 `lib/types/saved-search-api.ts` 신규 (★ API-005 영역, Request 1 + Response 2 + Geocoding + ErrorCode 5종 + ErrorDTO, base 에서 import)
- ✅ B.3 `lib/constants/saved-search-errors.ts` 신규 (SAVED_SEARCH_ERROR_MAP 5 키)
- ✅ B.4 `lib/helpers/saved-search-error.ts` 신규 (createSavedSearchError)
- ✅ B.5 `tsc --noEmit` exit 0 (회귀 0) + `prisma validate` exit 0 + 가드 8 그룹 무수정 (DiagnosisMode 11 + DiagnosisStatus 2 + types.ts + API-001/002/003 산출물 9 + DB-001~007 + SS1~SS3 + S1~S6 + N1~N6 + mock-auth M1~M4 grep)

### 후행 ISSUE 검증 (위임)

- **CMD-SAVE-001**: §3.3 `saveSearchRequestSchema` (SS1 통합) + AC-1/2 + ★ DB-006 §9.1 strict/passthrough 결정 + ★ userId 서버 사이드 추출 (REQ-NF-021)
- **QRY-SAVE-001 single owner**: §3.5 `mapSavedSearchToDTO` + `mapToGetResponse` (★ SS1 single inline 통합, ★ API-003 dual 과 다름) + AC-3/4 + ★ geocoding 재검증 시점 결정
- **TEST-005 + TEST-007**: §3.6/3.7/3.8 + 간이 시나리오 분배
- **INFRA-002**: SS2 lib/db.ts mock → 실 Prisma 전환
- **수동 검증** (CMD-SAVE / QRY-SAVE 시점):
  1. `tsc --noEmit` 으로 DTO ↔ Zod schema z.infer ↔ Prisma model 호환 재확인
  2. IDE 에서 `SavedSearchDTO` / `GetSavedSearchResponse` / `SearchParams` 자동완성 정상 동작
  3. Mock 모드 + 실 모드 모두 DTO 정합 확인

---

## 9. 🚧 Open Questions / Risks (보류 사항 — follow-up 7+ + ★ Wave 2 트랙 D 완성 마일스톤 § + ★ 3종 가이드 § 완성 정리 § + ★ DB-006 §7+§9.1 동시 트리거 수신 § + ★ Wave 2 체인 작동 4회째 §)

### ★ Wave 2 트랙 D 완성 마일스톤 § (★ 본 ISSUE 특화, Critical Path Wave 3 진입 트리거)

Wave 2 = API Contract 트랙 D (API-001/002/003/005 — API-004 Payment MVP 제외). 본 ISSUE 머지 시 트랙 D 완성:

| 시점 | ISSUE | 산출물 | 트랙 D 진척 |
|---|---|---|---|
| Wave 2 첫 진입 | API-001 PR #81 (`ee83429`) | auth.ts + auth-errors.ts + auth-error.ts (3 파일) + (P1) OAuthProvider=AuthProviderType + Phase B 부활 + L6 2차 정정 | 1/4 |
| Wave 2 두 번째 | API-002 PR #82 (`891ae97`) | diagnosis.ts (append) + diagnosis-errors.ts + diagnosis-error.ts (3 파일) + ★ Q2 9칸 가드 코드 사수 첫 성공 + L6 3차 cleanup 트리거 + (P1) CandidateAreaDTO re-export | 2/4 |
| Wave 2 세 번째 | API-003 PR #83 (`f0b012e`) | share-link.ts (신규 첫 owner) + share-link-errors.ts + share-link-error.ts (3 파일) + ★ Wave 2 체인 작동 첫 코드 입증 + (P1) 미묘 구분 § + S3+S4 dual mapper § | 3/4 |
| **★ Wave 2 마지막 (본 ISSUE)** | **API-005 본 ISSUE** | **saved-search.ts (DB-006 영역 base 신규 owner) + saved-search-api.ts (API 영역) + saved-search-errors.ts + saved-search-error.ts (★ 4 파일, API-003 +1) + ★ Q3 (가) 분리 strategy § + ★ mapper 패턴 차이 § + ★ 3종 가이드 § 완성 + ★ Wave 2 트랙 D 완성** | **4/4 ✅** |

**본 ISSUE 머지 후 Critical Path Wave 3 진입**:
- MOCK-001~005 (Mock 데이터 트랙)
- TEST-001~008 (단위 + GWT + 시나리오 + 보안 트랙)
- CMD-DIAG / CMD-AUTH / CMD-SHARE-001 / CMD-SAVE-001 (Command 트랙)
- QRY-DIAG-001 / QRY-SHARE-001 / QRY-SAVE-001 (Query 트랙)
- UI-007 / UI-014 (UI 트랙)
- cleanup ISSUE (REFACTOR-L6 — 트랙 D 동안 누적된 4 회 트리거 일괄 해소)

**Wave 2 트랙 D 의미**:
- DTO 통신 계약 SSoT 확립 (4 도메인 — Auth / Diagnosis / ShareLink / SavedSearch)
- 후행 Command/Query/Test 가 typed contract 위에서 작업 가능
- 1인 MVP 컨텍스트에서 Phase B 활성 + 커밋 2개 + 가드 사수 패턴 4 회 반복 정착
- (P1) 미묘 구분 + 분리/통합 strategy + mapper 패턴 차이 = 3종 가이드 § 완성 → Wave 3+ 새 도메인 결정 매트릭스

### ★ 3종 가이드 § 완성 정리 § (★ 본 ISSUE 신규 owner — Wave 3+ 새 도메인 ISSUE 결정 매트릭스)

Wave 2 트랙 D 동안 발견·정립된 3종 가이드. Wave 3+ 새 도메인 ISSUE 가 본 매트릭스 참조:

| 가이드 | owner ISSUE | 결정 변수 | 결정 매트릭스 |
|---|---|---|---|
| **(P1) 패턴 미묘 구분** | API-003 §9 (★ 본 ISSUE §2 4 회째 케이스 추가) | base type 존재 여부 | base 있음 → (P1) re-export 적용 (`OAuthProvider=AuthProviderType` / `CandidateAreaDTO=CandidateArea`) / base 0 → 자기 도메인 신규 정의 (Prisma 직접 노출 절대 금지) |
| **분리/통합 strategy** | **★ API-005 §9 (★ 본 ISSUE 신규 owner)** | DB-* §7 base 명시 + DB 영역 정의 위임 여부 | (i) base 명시 + DB 영역 직접 정의 → 분리 (DB-003 → API-002 + diagnosis-errors.ts) / (ii) base 명시 + DB 영역 위임 → 분리 (★ DB-006 → API-005 saved-search.ts base + saved-search-api.ts API) / (iii) base 0 + DTO 위임만 → 1 파일 통합 (DB-004 → API-003 share-link.ts) |
| **mapper 패턴 차이** | **★ API-005 §9 (★ 본 ISSUE 신규 owner)** | SSR 페이지 존재 여부 | Route only inline → single mapper (QRY-DIAG-001 / QRY-SAVE-001) / Route + SSR dual inline → dual mapper 단일 owner (분리 금지) (QRY-SHARE-001) |

**3종 가이드 § 의미 (★ 본 ISSUE 완성)**:
- Wave 2 트랙 D 4 ISSUE 의 누적 발견 = 3종 가이드 정립
- 미래 새 도메인 ISSUE (예: Notification DTO / Recommendation DTO 등) 가 본 매트릭스로 빠르게 결정 가능
- "결정 변수" 가 명확 (base 여부 / DB 영역 위임 여부 / SSR 존재 여부) → 모호한 판단 없이 매트릭스 lookup

### ★ DB-006 §7 + §9.1 동시 트리거 수신 § (★ 본 ISSUE 핵심 — 단일 ISSUE 가 두 트리거 동시 발신)

**DB-006 PR #79 §7 + §9.1 = 두 트리거 동시 발신** → **본 ISSUE 동시 수신**:

| 트리거 | DB-006 발신 시점 | 명세 명시 | 본 ISSUE 수신 처리 |
|---|---|---|---|
| **§7 — base type 정의 위임** | DB-006 PR #79 §7 "후행 ISSUE 정합성" 표 | "API-005 = `src/lib/types/saved-search.ts` 신규 + SearchParams interface + SavedSearchDTO 정의 + `import { ServiceModeType } from './user'` 패턴 (DB-003 Q6 답습)" | **본 ISSUE Phase B.1 직접 충족** — saved-search.ts 신규 owner + SearchParams + SavedSearchDTO + ServiceModeType import |
| **§9.1 — strict/passthrough 결정 위임** | DB-006 PR #79 §9.1 | "Zod 스키마를 `z.object().passthrough()` 로 설정하여 하위 호환성을 유지할지, strict 모드로 갈지 **CMD-SAVE-001 작업 시 확정**" | **본 ISSUE = DTO contract only, §3.3 CMD-SAVE-001 위임 발신** — CMD-SAVE-001 시점 결정 |

**동시 수신 의미 (★ 트리거 체인 작동 새 차원)**:
- API-002 §9.6 트리거 → API-003 단일 수신 (1 트리거)
- ★ DB-006 §7 + §9.1 → API-005 동시 수신 (2 트리거)
- DB → API 트리거 체인이 단일 트리거 아닌 멀티 트리거로 확장 작동 = mismatch 추적 정신 누적 입증

### ★ Wave 2 체인 작동 4회째 § (★ 본 ISSUE 다른 차원 입증 — 체인 다양성)

12+ ISSUE 누적 워크플로 = 격리된 칸 아닌 **체인 시스템** 입증. 본 ISSUE 가 4 회째 다른 차원 증거:

| # | 체인 | 시점 | 증거 |
|---|---|---|---|
| 1 | **Wave 1 → Wave 2** | DB-003 PR #77 (DiagnosisStatusType 정의, 사용처 0건) → API-002 PR #82 (DiagnosisDTO.status 첫 활성) | DB → API 트랙 |
| 2 | **Wave 2 내부 (API-003)** | API-002 PR #82 (CandidateAreaDTO re-export, 외부 활용 0) → API-003 PR #83 (ReportDTO.candidates: CandidateAreaDTO[] 첫 외부 활용) | API → API 내부 체인 |
| 3 | **트리거 → 위임 (API-003)** | API-002 §9.6 트리거 (validators/diagnosis.ts shareLink* 2종 = CMD-SHARE 영역) → API-003 §9.6 수신 + CMD-SHARE-001 §3.3 위임 발신 | mismatch 추적 정신 누적 |
| **★ 4** | **★ DB → API 차원 (API-005)** | **DB-006 §7 직접 명시 (saved-search.ts 신규 owner + SearchParams + SavedSearchDTO + ServiceModeType import) → ★ API-005 본 ISSUE base 정의 직접 충족 + §9.1 동시 수신** | **DB → API 차원 + 동시 multi 트리거 = 체인 다양성 입증** |

**입증 내용 (4 회째 다른 차원)**:
- 12+ ISSUE (INFRA-001 / DB-001~007 / API-001/002/003/005) 가 격리된 칸이 아닌 의존 체인으로 작동
- 체인이 단일 패턴 아닌 도메인 특성에 맞게 분기되는 시스템 입증 (DB→API / API→API / 트리거→위임 / multi 트리거)
- 향후 후행 ISSUE (CMD-DIAG / QRY-DIAG / CMD-SHARE / QRY-SHARE / CMD-SAVE / QRY-SAVE / TEST-* / cleanup ISSUE) 가 본 체인 답습 가능

### 1. ★ §3.3 CMD-SAVE-001 위임 — `saveSearchRequestSchema` 통합

CMD-SAVE-001 시점에:
- SS1 `api/save/route.ts:5-7` `z.record(z.string(), z.unknown())` passthrough-like 인라인 → 추출 또는 보존 결정
- ★ **DB-006 §9.1 strict/passthrough 최종 결정** (`z.object().passthrough()` vs `z.object().strict()`) — 하위 호환성 vs 타입 안전성 트레이드오프
- Route Handler L13 `safeParse` 호출처 SS1 통합
- B.2 `SaveSearchRequest` import 정합 + `SavedSearchErrorCode.INVALID_PARAMS` 활용
- AC-1 / AC-2 검증 활성 (TEST-005 시점)

### 2. ★ §3.5 QRY-SAVE-001 single owner 위임 — `mapSavedSearchToDTO` + `mapToGetResponse` 추출 (★ API-003 dual 과 다름, ★ API-002 N3 single 과 동일)

QRY-SAVE-001 시점에:
- `lib/mappers/saved-search-mapper.ts` 신규: `mapSavedSearchToDTO(record: PrismaSavedSearch): SavedSearchDTO` + `mapToGetResponse(record: PrismaSavedSearch | null, geocodingStatus?): GetSavedSearchResponse`
- ★ **SS1 L49 findUnique + L55-61 인라인 mapper → single mapper 추출 + 호출처 통합** (★ API-002 N3 동일 패턴, ★ API-003 S3+S4 dual 과 다름 — mapper 패턴 차이 § 참조)
- B.2 `GetSavedSearchResponse` + B.1 `SavedSearchDTO` / `SearchParams` 활용
- ★ **geocoding 재검증 시점 결정** — server-side (QRY-SAVE-001 내부 호출) vs client-side (별도 API 호출) — TS 타입은 optional 로 유연
- Sentry catch + `SavedSearchErrorCode.NOT_FOUND` / `GEOCODING_FAILED` 활용
- AC-3 / AC-4 검증 활성 (TEST-005/007 시점)

### 3. ★ §3.6/3.7/3.8 TEST-005 + TEST-007 분배 위임

TEST-005 (저장 GWT) 시점에:
- `vitest.config.ts` 신규 (DB-001~007 §3.10 + API-001 §3.7/3.8 + API-002 §3.6/3.7/3.8 + API-003 §3.6/3.7/3.8 + 본 ISSUE §3.6/3.7/3.8 일괄 위임 누적 해소)
- `__tests__/types/saved-search-api.spec.ts` 4 케이스 (SaveSearchRequest 정합 / GetSavedSearchResponse.found false 케이스 / SearchParams.mode = ServiceModeType / SavedSearchErrorCode 전수 매핑)
- `__tests__/validators/saved-search-api.spec.ts` 5 케이스 (정상 / searchParams 누락 / mode 무효 / 빈 객체 / DB-006 §9.1 passthrough vs strict)
- `__tests__/mappers/saved-search-mapper.spec.ts` 3 케이스 (정상 / null record / geocoding 실패)
- `__tests__/helpers/saved-search-error.spec.ts` 3 케이스 (API-001/002/003 helper spec 패턴 추가)
- 저장 GWT E2E (REQ-FUNC-025 통합 — POST /api/save → upsert → GET /api/save → 복원)

TEST-007 (간이 시나리오) 시점에:
- 저장 → 복원 → geocoding 재검증 → "주소를 다시 입력해주세요" 안내 (REQ-FUNC-025 1초 + REQ-NF-016 best effort 통합)

### 4. ★ Q10 — DB-006 §9.1 strict/passthrough CMD-SAVE-001 영역 명확화 (본 ISSUE 미수정)

본 ISSUE 미수정. CMD-SAVE-001 시점에:
- DB-006 §9.1 자체가 CMD-SAVE-001 위임 명시 — 본 ISSUE 가 트리거 수신 → CMD-SAVE-001 위임 발신
- SS1 `z.record(z.string(), z.unknown())` passthrough-like 인라인 등가 동작 통합
- B.2 `SaveSearchRequest.searchParams: SearchParams` 정의 ↔ Zod schema z.infer 정합 확인

### 5. ★ userId 서버 사이드 추출 (REQ-NF-021) — CMD-SAVE-001 영역 (본 ISSUE = DTO contract only)

본 ISSUE B.2 `SaveSearchRequest` 에 userId 필드 절대 금지 + 주석 명시 "Supabase Auth 세션 서버 사이드 추출 (클라이언트 전달 금지, REQ-NF-021)".
CMD-SAVE-001 시점에:
- Supabase Auth `createServerClient` + `getUser()` 호출로 세션에서 userId 추출
- SS1 `userId = "mock-user-001"` 인라인 → 실 세션 userId 치환
- 비인가 접근 시 `SavedSearchErrorCode.SESSION_REQUIRED` 반환 (HTTP 401)

### 6. ★ SearchParams 스키마 진화 — DB-006 §9.1 동기화

CMD-SAVE-001 시점에 DB-006 §9.1 strict/passthrough 결정 후:
- strict 채택 시: B.1 `SearchParams` interface 의 모든 optional 필드를 Zod schema 와 정합 (필드 추가 시 양쪽 동기화 필수)
- passthrough 채택 시: B.1 `SearchParams` interface 는 known 필드만, Zod 가 unknown 필드 통과 허용
- 향후 새 필드 추가 (예: 학군 필드, 야간 치안 필드 등) 시 양쪽 일관 유지

### 7. ★ geocoding 재검증 시점 — QRY-SAVE-001 영역

QRY-SAVE-001 시점에:
- 옵션 A: server-side (QRY-SAVE-001 내부에서 카카오 Geocoding API 호출 후 GetSavedSearchResponse.geocodingStatus 채움)
- 옵션 B: client-side (별도 클라이언트 호출, geocodingStatus 옵셔널 미사용)
- B.2 `GetSavedSearchResponse.geocodingStatus?` optional 로 양쪽 지원
- 1초 복원 (REQ-FUNC-025) 위해 server-side 캐싱 또는 client-side 비동기 권장 → QRY-SAVE-001 결정

### 추가 보조 (명세 §9 v1.0 보존)

- **DiagnosisFilters 일관**: B.1 `SearchParams.filters` 가 `lib/types.ts:37-42` `DiagnosisFilters` 와 동일 구조 (maxCommuteTime/budget 객체/timeRange enum/priorities) — types.ts 가드 보존 위해 형태만 평행 정의. 향후 cleanup ISSUE 시점에 import 또는 통합 검토.
- **Figma leisure 4 필드 superset**: B.1 `SearchParams.leisureA/leisureCoordA/leisureB/leisureCoordB` 4 필드 = single 모드 여가거점 superset. DiagnosisInput 과 다른 SavedSearch 특화 필드.
- **SS2 lib/db.ts → INFRA-002 진짜 DB**: 7 호출처 mock storage → 실 Prisma 전환 시 동작 검증 필수. RLS 도 INFRA-002+DB-007 영역.
- **1:0..1 (found false 정상 케이스 vs NOT_FOUND 에러 구분)**: GetSavedSearchResponse.found false = 정상 (저장된 기록 없음, 첫 방문) / SavedSearchErrorCode.NOT_FOUND = 에러 (조회 자체 실패). UI-014 시점 명확 구분 필수.
